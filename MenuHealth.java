import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class MenuHealth here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class MenuHealth extends Actor
{
    public MenuHealth(){
        GreenfootImage image = getImage();
        image.scale(75, 75);
        setImage(image);
    }
    /**
     * Act - do whatever the MenuHealth wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }    
}
